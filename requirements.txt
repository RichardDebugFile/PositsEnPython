# Dependencias principales (incluidas en Python)
# tkinter - Interfaz gráfica (incluido en Python)
# json, os, uuid, datetime, pathlib, urllib, logging, base64 - Estándar de Python

# Dependencias opcionales para funcionalidades avanzadas

# Reconocimiento de voz
SpeechRecognition>=3.10.0
pyaudio>=0.2.11

# Reconocimiento de voz local (más rápido)
faster-whisper>=0.9.0

# Para desarrollo y testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Nota: En Windows, pyaudio puede requerir instalación especial:
# pip install pipwin
# pipwin install pyaudio 